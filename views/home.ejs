<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>

<body>
    <h2>Home <%= locals.data.name %>
    </h2>

    <table>
        <tr>
            <td>Name</td>
            <td>
                <%= locals.data.name %>
            </td>
        </tr>
        <tr>
            <td>Email</td>
            <td>
                <%= locals.data.email %>
            </td>
        </tr>
        <tr>
            <td>Phone Number</td>
            <td>
                <%= locals.data.phoneNum %>
            </td>
        </tr>
    </table>
    <div>
        <a href="/edit/<%= locals.data._id %>">Edit</a>
    </div>
    <div>
        <% if (locals.msg) { %>
            <%= locals.msg %>
                <% } %>

    </div>
    <a href="/logout">logout</a>
</body>

</html>